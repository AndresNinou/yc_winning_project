# Claude Backend Configuration
# Copy this file to .env.local for local development

# Backend URL - defaults to the provided Claude service
CLAUDE_BACKEND_URL=https://integral-mozilla-ref-db.trycloudflare.com

# Optional: Override for local development
# CLAUDE_BACKEND_URL=http://localhost:8000

# Optional: Override for production deployment
# CLAUDE_BACKEND_URL=https://your-production-backend.com

# Note: The backend provides the following tools:
# - ListDir, Read, Write, Search, StrReplace, CreateFile, ViewFile, Bash
# - Each conversation gets its own isolated workspace
# - Real-time streaming events for build progress

